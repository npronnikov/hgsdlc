spring:
  datasource:
    url: jdbc:h2:mem:hgsdlc_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

auth:
  seed-username: test
  seed-password: test
  seed-role: FLOW_CONFIGURATOR
